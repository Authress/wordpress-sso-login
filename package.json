{
  "name": "authress-wordpress-plugin",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "./composer.phar install -d authress --prefer-dist --optimize-autoloader",
    "build": "./composer.phar install -d authress --prefer-dist --optimize-autoloader",
    "start": "sudo docker-compose up",
    "test": "./authress/vendor/bin/phpcs -i && ./authress/vendor/bin/phpcs -s authress",
    "kill": "sudo docker-compose rm",
    "cli": "sudo docker-compose run wordpress /bin/bash",
    "package": "zip authress.zip -r authress"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "authress-sdk": "^1",
    "base64url": "^3.0.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "header-case-normalizer": "^1.0.3",
    "json-stringify-safe": "^5.0.1",
    "lodash": "^4.17.21",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  }
}
